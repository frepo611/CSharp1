namespace MinaPrylar;

public class Car
{
    private string _brand;
    private string _color;
    public Car(string brand, string color)
    {
        _brand = brand;
        _color = color;
    }
}